% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/smoothbp_ss.R
\name{update.smoothbp_ss_fit}
\alias{update.smoothbp_ss_fit}
\title{Update a fitted spike-and-slab smoothbp model}
\usage{
\method{update}{smoothbp_ss_fit}(
  object,
  formula,
  b0,
  b1,
  deltas,
  omega,
  rho,
  data,
  priors,
  spike,
  b1_spike,
  hierarchical,
  chains,
  iter,
  warmup,
  seed,
  step_om,
  step_rho,
  target_accept,
  cores,
  .verbose = TRUE,
  ...
)
}
\arguments{
\item{object}{A \code{smoothbp_ss_fit} object.}

\item{formula, b0, b1, deltas, omega, rho, data, priors, spike, b1_spike, hierarchical, chains, iter, warmup, seed, step_om, step_rho, target_accept, cores, .verbose}{Replacements for the corresponding arguments of \code{\link{smoothbp_ss}}.
Any argument not supplied is taken from \code{object}, including the
original \code{seed} and sampler tuning parameters. For fits saved before
\code{b1_spike} was stored on the object, it is inferred from whether any
\code{gamma_b1_} indicator draw ever left the slab (they are frozen at 1
when \code{b1_spike = FALSE}).}

\item{...}{Ignored.}
}
\value{
A new \code{smoothbp_ss_fit} object.
}
\description{
Re-fits the model with \code{\link{smoothbp_ss}}, replacing any arguments
supplied here with the corresponding values stored in the original fit for
anything left unspecified. Without this method, \code{update()} on a
spike-and-slab fit would fall through to \code{update.smoothbp_fit()} and
silently refit the model without the spike-and-slab structure.
}
