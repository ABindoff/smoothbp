% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bridge_sampling.R
\name{bridge_sampler.smoothbp_fit}
\alias{bridge_sampler.smoothbp_fit}
\title{Bridge sampling for smoothbp_fit objects}
\usage{
\method{bridge_sampler}{smoothbp_fit}(
  samples,
  repetitions = 1,
  method = "normal",
  cores = 1,
  use_neff = TRUE,
  maxiter = 1000,
  silent = FALSE,
  verbose = FALSE,
  ...
)
}
\arguments{
\item{samples}{A \code{smoothbp_fit} object.}

\item{repetitions}{Number of bridge-sampling repetitions. Default \code{1}.}

\item{method}{Bridge-sampling method: \code{"normal"} (default) or \code{"warp3"}.}

\item{cores}{Number of cores for evaluating the log posterior. Default \code{1}.}

\item{use_neff}{Logical; use effective sample size in bridge function.
Default \code{TRUE}.}

\item{maxiter}{Maximum iterations for the iterative scheme. Default \code{1000}.}

\item{silent}{Logical; suppress iteration printing. Default \code{FALSE}.}

\item{verbose}{Logical; print internal debug info. Default \code{FALSE}.}

\item{...}{Additional arguments passed to
\code{\link[bridgesampling:bridge_sampler]{bridgesampling::bridge_sampler()}}.}
}
\value{
A \code{bridge} object from the \strong{bridgesampling} package. Use
\code{\link[bridgesampling:logml]{bridgesampling::logml()}} to extract the log marginal likelihood and
\code{\link[bridgesampling:bf]{bridgesampling::bayes_factor()}} to compare two models.
}
\description{
Computes the log marginal likelihood of a \code{smoothbp_fit} model via bridge
sampling. The result is a \code{bridge} object compatible with
\code{\link[bridgesampling:bf]{bridgesampling::bayes_factor()}}, allowing direct comparison with other
Bayesian models including those fitted with \strong{brms}.
}
\details{
Bridge sampling requires evaluating the log unnormalized posterior density
at each posterior draw. Internally, this function reconstructs the model's
linear predictors in R using the stored design matrices and computes the
sum of the log likelihood and all log prior densities.

For comparison with a \strong{brms} model, the brms model must have been fitted
with \code{save_pars = save_pars(all = TRUE)}.
\subsection{Checking reliability}{

Bridge sampling is a stochastic approximation.  Always check the
uncertainty of the estimate with \code{\link[bridgesampling:error_measures]{bridgesampling::error_measures()}}:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{bs <- bridge_sampler(fit)
error_measures(bs)  # inspect $cv (coefficient of variation)
}\if{html}{\out{</div>}}

A coefficient of variation (\verb{$cv}) below 0.05 (5\%) indicates a reliable
estimate.  Values above ~0.15 suggest the posterior was poorly explored
— check \code{trace_plot(fit)} and consider increasing \code{iter}.  When
convergence is poor, \code{\link[=loo]{loo()}} is a more robust comparison tool.
}
}
\examples{
\dontrun{
library(brms)

fit_piece  <- smoothbp(y ~ tau, b0 = ~1 + (1|subject), data = d)
fit_linear <- brm(y ~ tau + (1|subject), data = d,
                  save_pars = save_pars(all = TRUE))

# Compute log marginal likelihoods
bs_piece  <- bridge_sampler(fit_piece)
bs_linear <- bridge_sampler(fit_linear)

# Assess reliability of the estimate before interpreting
bridgesampling::error_measures(bs_piece)   # check $cv < 0.05 ideally

# Bayes factor: piecewise vs linear
bridgesampling::bayes_factor(bs_piece, bs_linear)
}
}
