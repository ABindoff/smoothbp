% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/smoothbp_ss.R
\name{smoothbp_ss}
\alias{smoothbp_ss}
\title{Fit a smooth change-point model with spike-and-slab variable selection on b2}
\usage{
smoothbp_ss(
  formula,
  b0 = ~1,
  b1 = ~1,
  b2 = ~1,
  omega = ~1,
  rho = ~1,
  data,
  priors = smoothbp_priors(),
  spike = prior_spike_slab(),
  chains = 4L,
  iter = 2000L,
  warmup = 1000L,
  seed = NULL,
  step_om = 0.3,
  step_rho = 0.3,
  target_accept = 0.65,
  cores = getOption("smoothbp.cores", 1L),
  .verbose = TRUE
)
}
\arguments{
\item{formula}{A two-sided formula identifying the response and time variable,
e.g. \code{value ~ tau}.}

\item{b0}{One-sided formula for the \eqn{b0} linear predictor.
May include a single random-intercept term, e.g.
\code{~ 1 + age + sex + (1 | subject_id)}.}

\item{b1}{One-sided formula for \eqn{b1}. Default \code{~ 1}.}

\item{b2}{One-sided formula for \eqn{b2}. Default \code{~ 1}.}

\item{omega}{One-sided formula for the change-point \eqn{\omega}. Default \code{~ 1}.}

\item{rho}{One-sided formula for the sharpness \eqn{\rho}. Default \code{~ 1}.}

\item{data}{A data frame containing all variables referenced in formulas.}

\item{priors}{A \code{\link{smoothbp_priors}} object. Defaults to
\code{smoothbp_priors()}.}

\item{spike}{A \code{\link[=prior_spike_slab]{prior_spike_slab()}} object controlling which \code{b2}
coefficients receive the spike-and-slab prior, the inclusion probability
\code{pi}, and the slab distribution. Default: \code{prior_spike_slab()} (pi = 0.5,
slab = Normal(0, 2), spike on non-intercept coefficients only).}

\item{chains}{Number of independent MCMC chains. Default 4.}

\item{iter}{Total iterations per chain (warmup + sampling). Default 2000.}

\item{warmup}{Number of warmup iterations (discarded). Default 1000.}

\item{seed}{Integer random seed for reproducibility.}

\item{step_om}{Initial step size for \eqn{\omega} coefficients.  When the
omega linear predictor has a single column (\code{omega = ~ 1}), this is
the scalar MH proposal SD.  When the predictor has \eqn{p \ge 2} columns,
this is the initial HMC leapfrog step size (tuned automatically via dual
averaging during warmup). Default 0.3.}

\item{step_rho}{Initial step size for \eqn{\rho} coefficients.  Same
semantics as \code{step_om}. Default 0.3.}

\item{target_accept}{Target acceptance probability for the HMC dual-averaging
step-size adaptation.  Only used when \code{omega} or \code{rho} has two
or more predictor columns; ignored for intercept-only specifications.
Recommended range \code{[0.6, 0.85]}.  Higher values produce smaller step
sizes and more conservative trajectories. Default 0.65.}

\item{cores}{Number of CPU cores used to run chains in parallel.  When
\code{cores > 1} all chains run concurrently via Rayon (the per-iteration
progress bar is suppressed in this mode — only a start and done message
are printed).  Defaults to \code{getOption("smoothbp.cores", 1L)}.  Set
\code{options(smoothbp.cores = parallel::detectCores())} in your
\code{.Rprofile} to make parallel the default.}

\item{.verbose}{Logical; print progress messages. Default \code{TRUE}.}
}
\value{
A \code{smoothbp_ss_fit} object (extends \code{smoothbp_fit}) with additional
components:
\describe{
\item{\code{gamma_names}}{Character vector of names for the gamma indicators.}
\item{\code{spike}}{The \code{prior_spike_slab} object used.}
}
The draws array includes gamma indicator columns (\verb{gamma_<coef_name>})
whose posterior means are the posterior inclusion probabilities (PIPs).
}
\description{
Like \code{\link[=smoothbp]{smoothbp()}}, but adds spike-and-slab priors on \code{b2} coefficients to
perform structured variable selection.
}
\details{
When a \code{b2} coefficient is "spiked" to zero (\eqn{\gamma_k = 0}), the
corresponding omega and rho coefficients (matched by column name) are also
set to zero, reflecting the idea that if a covariate does not affect the
slope change, the change-point location/sharpness should not vary with that
covariate either.

The sampler uses the Kuo & Mallick (1998) approach: all regression
coefficients are always sampled (even when "excluded"), but only the
included ones contribute to the mean function.  Inclusion indicators
\eqn{\gamma_k} are sampled from their Bernoulli full conditional each
iteration.
}
\examples{
\dontrun{
dat <- simulate_smoothbp(
  n_subj = 1, n_obs = 60,
  b0 = 5, b1 = -0.4, b2 = 1.2,
  omega = 3, rho = 4, sigma = 0.4, sigma_u = 0
)
# Add a covariate with no true effect on b2
dat$x <- rnorm(nrow(dat))

fit <- smoothbp_ss(
  formula = y ~ tau,
  b2    = ~ 1 + x,
  omega = ~ 1 + x,
  data  = dat,
  spike = prior_spike_slab(pi = 0.5)
)
pip(fit)
}

}
\references{
Kuo, L. and Mallick, B. (1998). Variable selection for regression models.
\emph{Sankhya B}, 60(1):65--81.
}
